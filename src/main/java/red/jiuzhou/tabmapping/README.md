# 表映射

本包包含管理客户端和服务器数据结构之间表映射的工具。

## 主要组件
- `MappingLoader.java` - 表映射配置加载器
- `SqlConverter.java` - SQL 转换工具
- `TableMapping.java` - 表映射定义

## 关键功能

- 客户端-服务器表映射
- 基于映射的 SQL 转换
- 基于配置的映射管理
- 字段关系定义

## 用途

本包处理不同数据结构之间的映射，特别适用于同步具有不同字段名称和关系的客户端和服务器数据模型。